apiVersion: apps/v1 # i se Usa apps/v1beta2 para versiones anteriores a 1.9.0
kind: Deployment
metadata:
  name: idc-servicio-censo
  labels:
    name: "servicio-censo"
spec:
  selector:   #permite seleccionar un conjunto de objetos que cumplan las condicione
    matchLabels:
      app: censo
  replicas: 1
  strategy:
    type: RollingUpdate
  template:   # Plantilla que define el container
    metadata:
      labels:
        app: censo
    spec:
      containers:
      - name: idc-servicio-censo
        image: dmcontainerregister.azurecr.io/idc-servicio-censo-dev:1.0.0.31
        ports:
          - containerPort: 8512
        env:
          - name: SPRING_PROFILES_ACTIVE
            value: k8s-dev
          - name: SERVER_PORT
            value: "8512"
      imagePullSecrets:
        - name: regcred
      nodeSelector:
        nodo: master

