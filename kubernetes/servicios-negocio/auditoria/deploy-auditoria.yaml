apiVersion: apps/v1 # i se Usa apps/v1beta2 para versiones anteriores a 1.9.0
kind: Deployment
metadata:
  name: idc-servicio-auditoria
  labels:
    name: "idc-servicio-auditoria"
spec:
  selector:   #permite seleccionar un conjunto de objetos que cumplan las condicione
    matchLabels:
      app: auditoria
  replicas: 1
  strategy:
    type: RollingUpdate
  template:   # Plantilla que define el container
    metadata:
      labels:
        app: auditoria
    spec:
      containers:
      - name: idc-servicio-auditoria
        image: dmcontainerregister.azurecr.io/idc-servicio-auditoria-dev:1.0.0.42
        ports:
          - containerPort: 8510
        env:
          - name: SPRING_PROFILES_ACTIVE
            value: k8s-dev
          - name: SERVER_PORT
            value: "8510"
      imagePullSecrets:
        - name: regcred
      nodeSelector:
        nodo: master

