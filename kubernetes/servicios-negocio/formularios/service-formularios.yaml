apiVersion: v1
kind: Service
metadata:
  name: idc-servicio-formularios
  labels:
     app: formularios
spec:
  ports:
  - port: 8522
    targetPort: 8522
    protocol: TCP
  selector:
     app: formularios

